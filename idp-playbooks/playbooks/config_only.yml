---
# Config-only run (configure + start + replication verify; no install)
- name: Configure, start, and verify only
  hosts: pingds_config
  roles:
    - pingds_configure
    - pingds_start
    - pingds_replication_verify
